# Куда Vite проксирует запросы с префикса /api.
# Реальный бэкенд (по умолчанию): http://localhost:3000
# Prism (мок по контракту):        http://localhost:4010
VITE_API_PROXY_TARGET=http://localhost:3000
